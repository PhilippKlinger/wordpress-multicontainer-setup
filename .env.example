# Copy this file to .env and adapt every value to the selected project.
# Never commit the real .env file.

APP_NAME=devsecops-app
APP_IMAGE_NAME=devsecops-app
APP_VERSION=latest

BASE_IMAGE=alpine:TODO_VERSION

HOST_PORT=TODO_HOST_PORT
CONTAINER_PORT=TODO_CONTAINER_PORT

DATA_DIR=/data

# Add project-specific, non-sensitive configuration below.
# Keep credentials empty in this example and define them only in .env.
APP_USERNAME=
APP_PASSWORD=
